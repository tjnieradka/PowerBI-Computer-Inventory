

Select tables and views. Additional tables may be added later, if needed. Select Transform Data.

Clean up and remove columns that have "Table" or "Value" values in the rows. Only columns with IDs or descriptive field names are kept.
image
Select Model view in Power BI Desktop, and validate or modify relationships. Remove unnecessary tables. In the example vW_* were removed since raw tables will be used. the extra view tables would have created confusing duplicate logic.
image
At this step, we can start creating reports in Report View.
